# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#
#
# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

#set $mod Mod4
set $mod Mod1

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex --autostart --environment i3

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
# exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock -i /home/chloe/Pictures/Wallpapers/roxy.png --nofork

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
#bindsym XF86AudioMute exec --no-startup-id wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle && $refresh_i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+ && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%- && $refresh_i3status
#bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
#bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status
#bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
#bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status

bindsym XF86AudioPause exec --no-startup-id playerctl play-pause
bindsym XF86AudioMute exec --no-startup-id playerctl next

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod


set $virtmgr "sh /home/chloe/Documents/QEMU-KVM/openVirtManager.sh"

set $hidemouse "sh -c 'eval `xdotool getdisplaygeometry --shell`; xdotool mousemove $((WIDTH/2)) $((HEIGHT))'"
set $movemouse "sh -c 'eval `xdotool getactivewindow getwindowgeometry --shell`; xdotool mousemove $((X+WIDTH/2)) $((Y+HEIGHT/16))'"
set $delaymovemouse "sh -c 'xdotool sleep 0.5; eval `xdotool getactivewindow getwindowgeometry --shell`; xdotool mousemove $((X+WIDTH/2)) $((Y+HEIGHT/16))'"


# start a terminal
bindsym Mod1+Control+t exec i3-sensible-terminal; exec --no-startup-id $delaymovemouse
bindsym $mod+Return exec i3-sensible-terminal; exec --no-startup-id $delaymovemouse
bindsym $mod+Shift+c split toggle, exec "urxvt"; exec --no-startup-id $delaymovemouse
bindsym $mod+Shift+f exec librewolf; exec --no-startup-id $delaymovemouse
# bindsym $mod+Shift+f [class="URxvt"] exec urxvt

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
bindsym $mod+q exec --no-startup-id dmenu_run
# A more modern dmenu replacement is rofi:
# bindcode $mod+40 exec "rofi -modi drun,run -show drun"
# There also is i3-dmenu-desktop which only displays applications shipping a
# .desktop file. It is a wrapper around dmenu, so you need that installed.
# bindcode $mod+40 exec --no-startup-id i3-dmenu-desktop


# change focus
bindsym $mod+j focus left;  exec --no-startup-id $movemouse
bindsym $mod+k focus down;  exec --no-startup-id $movemouse
bindsym $mod+i focus up;    exec --no-startup-id $movemouse
bindsym $mod+l focus right; exec --no-startup-id $movemouse

bindsym $mod+Tab          focus prev; exec --no-startup-id $movemouse
bindsym $mod+Shift+Tab    focus parent, focus prev, focus child; exec --no-startup-id $movemouse
bindsym $mod+Caps_Lock    focus parent, focus prev, focus child; exec --no-startup-id $movemouse
#bindsym Mod4+Caps_Lock    focus parent, focus prev, focus child; exec --no-startup-id $movemouse
#bindsym $mod+ordmasculine focus parent, focus prev, focus child; exec --no-startup-id $movemouse
#bindsym $mod+bar          focus parent, focus prev, focus child; exec --no-startup-id $movemouse

bindsym $mod+Control+Tab focus parent, focus prev, focus child, focus prev, focus parent, focus parent, focus child, mark --add "swapee", focus prev, swap container with mark "swapee", unmark "swapee"; exec --no-startup-id $movemouse
bindsym $mod+Control+Shift+Tab mark --add "swapee", focus prev, focus parent, focus prev, focus child, focus parent, focus prev, focus child, focus parent, focus prev, focus child, focus parent, focus prev, focus child, swap container with mark "swapee", unmark "swapee"; exec --no-startup-id $movemouse

#bindsym $mod+Caps_Lock    workspace next
bindsym $mod+bar          workspace next
bindsym $mod+ordmasculine workspace next
bindsym Mod4+ordmasculine workspace next
bindsym Mod4+Caps_Lock    workspace next
bindsym Mod4+bar          workspace next
bindsym Mod4+Tab          workspace next
#bindsym Mod4              workspace next

#bindsym $mod+Control+Shift+Tab mark --add "swapee", focus prev, swap container with mark "swapee", unmark "swapee", focus prev
#bindsym $mod+Control+Tab focus parent, focus prev, focus child, mark --add "swapee", focus prev, focus parent, swap container with mark "swapee", unmark "swapee", focus prev

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left; exec --no-startup-id $movemouse
bindsym $mod+Down focus down; exec --no-startup-id $movemouse
bindsym $mod+Up focus up; exec --no-startup-id $movemouse
bindsym $mod+Right focus right; exec --no-startup-id $movemouse

# move focused window
bindsym $mod+Shift+j move left; exec --no-startup-id $movemouse
bindsym $mod+Shift+k move down; exec --no-startup-id $movemouse
bindsym $mod+Shift+i move up; exec --no-startup-id $movemouse
bindsym $mod+Shift+l move right; exec --no-startup-id $movemouse

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left; exec --no-startup-id $movemouse
bindsym $mod+Shift+Down move down; exec --no-startup-id $movemouse
bindsym $mod+Shift+Up move up; exec --no-startup-id $movemouse
bindsym $mod+Shift+Right move right; exec --no-startup-id $movemouse

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle; exec --no-startup-id $movemouse

# change container layout (stacked, tabbed, toggle split)
# bindsym $mod+s layout stacking
# bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle; exec --no-startup-id $movemouse

# focus the parent container
# bindsym $mod+a focus parent

# focus the child container
# bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws0 "0"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws0

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1, workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2, workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3, workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4, workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5, workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6, workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7, workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8, workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9, workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws0, workspace number $ws0

workspace $ws1 output primary
workspace $ws2 output primary
workspace $ws3 output primary
workspace $ws4 output secondary
workspace $ws5 output primary
workspace $ws6 output primary
workspace $ws7 output primary
workspace $ws8 output primary
workspace $ws9 output primary
workspace $ws0 output secondary

# reload the configuration file
#bindsym $mod+Shift+c reload

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

#set $bg-color            #9A7ECC
#set $bg-color            #EBDBB2

# VIOLET TOKYO NIGHT
#set $bg-color            #C678DD
# VIOLET TOKYO NIGHT

# GREEN GRUVBOX
#set $bg-color            #8EC07C
# GREEN GRUVBOX

#xrdb ~/.Xresources
#xrdb ~/.Xdefaults
set_from_resource $color-black-normal   i3wm.color0  #000000
set_from_resource $color-black-bright   i3wm.color8  #000000
set_from_resource $color-red-normal     i3wm.color1  #FF0000
set_from_resource $color-red-bright     i3wm.color9  #FF0000
set_from_resource $color-yellow-normal  i3wm.color2  #FFFF00
set_from_resource $color-yellow-bright  i3wm.color10 #FFFF00
set_from_resource $color-green-normal   i3wm.color3  #00FF00
set_from_resource $color-green-bright   i3wm.color11 #00FF00
set_from_resource $color-blue-normal    i3wm.color4  #0000FF
set_from_resource $color-blue-bright    i3wm.color12 #0000FF
set_from_resource $color-magenta-normal i3wm.color5  #FF00FF
set_from_resource $color-magenta-bright i3wm.color13 #FF00FF
set_from_resource $color-cyan-normal    i3wm.color6  #00FFFF
set_from_resource $color-cyan-bright    i3wm.color14 #00FFFF
set_from_resource $color-white-normal   i3wm.color7  #FFFFFF
set_from_resource $color-white-bright   i3wm.color15 #FFFFFF


#set $bg-color            #FF80FF
#set $inactive-bg-color   #555555
#set $text-color          #FF80FF
#set $inactive-text-color #FF80FF
#set $urgent-bg-color     #FF80FF
#set $indicator-color     #FF80FF
#set $border-color        #FF80FF
#set $border-active-color #FF80FF
# window colors
#                       border               background         text                 indicator
#client.focused          $border-active-color $bg-color          $text-color          $border-active-color
#client.unfocused        $border-color        $inactive-bg-color $inactive-text-color $border-active-color
#client.focused-inactive $border-color        $inactive-bg-color $inactive-text-color $border-active-color
#client.urgent           $border-color        $urgent-bg-color   $text-color          $border-active-color


#set $frame-color       #FFB0FF
set $frame-color       #BBBBBB
set $inactive-bg-color #666666

# window colors
#                       border              background         text                 indicator
client.focused          $frame-color        $frame-color       $frame-color         $frame-color
client.unfocused        $frame-color        $inactive-bg-color $frame-color         $frame-color
client.focused-inactive $frame-color        $inactive-bg-color $frame-color         $frame-color
client.urgent           $frame-color        $frame-color       $frame-color         $frame-color


# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
	# mode invisible
	mode hide
	position bottom
	status_command i3status
}

workspace $ws1 gaps vertical 200px
workspace $ws1 gaps horizontal 8px
workspace_auto_back_and_forth no

#gaps inner 8px
#gaps outer -8px
gaps inner 0px
gaps outer 0px
smart_gaps inverse_outer

default_border none
default_floating_border none
#default_floating_border pixel 2

#default_border pixel 4
#default_floating_border pixel 4

#hide_edge_borders horizontal
#hide_edge_borders vertical
#hide_edge_borders both
#hide_edge_borders smart
hide_edge_borders smart_no_gaps

#exec_always --no-startup-id feh --bg-fill ~/Documents/pictures/wallpapers/drift.jpg
exec_always --no-startup-id feh --bg-fill ~/Documents/pictures/wallpapers/frieren.jpg
#exec_always --no-startup-id feh --bg-fill ~/Documents/pictures/wallpapers/cyberpunk.jpg
#exec_always --no-startup-id feh --bg-fill ~/Documents/pictures/wallpapers/landscape.jpg
exec_always --no-startup-id nitrogen --restore
exec_always --no-startup-id picom
exec_always --no-startup-id xset -dpms # disables Display Power Management Signaling
exec_always --no-startup-id xset s off # disables Display Power Management Signaling
#exec_always --no-startup-id picom -CGb

#exec_always --no-startup-id setxkbmap latam
#exec_always --no-startup-id setxkbmap en
exec_always --no-startup-id setxkbmap es
exec_always --no-startup-id $virtmgr

# for_window [class="URxvt"] floating enable
# for_window [class="firefox"] move to workspace $ws2

for_window [class="eog"] floating enable
for_window [class="Eog"] floating enable
for_window [class="OSD"] move to workspace $ws4
for_window [class="java"] move to workspace $ws2
for_window [class="Java"] move to workspace $ws2
for_window [class="Eclipse"] move to workspace $ws2
for_window [class="virt-manager"] move to workspace $ws5
for_window [class="Virt-manager"] move to workspace $ws5
for_window [class="virt-manager" floating] border pixel 2
for_window [class="Virt-manager" floating] border pixel 2
for_window [class="Gnome-terminal"] floating enable
for_window [class="gnome-calculator"] floating enable


focus_wrapping force
default_orientation vertical

